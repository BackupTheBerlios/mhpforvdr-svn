
package org.havi.ui.event;

/*The HKeyListener interface enables the reception of HKeyEvent events. Note that HAVi 
compliant systems should never call the methods keyReleased(KeyEvent)and 
keyTyped(KeyEvent)of the superinterface on an HKeyListener .The parameters to the 
constructors are as follows,in cases where parameters are not used,then the constructor 
should use the default values. */

public interface HKeyListener extends java.awt.event.KeyListener {


}
